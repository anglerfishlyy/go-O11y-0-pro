// go mod init go-o11y   // creates a new module for your project
// go mod tidy           // cleans and adds needed dependencies
// go build              // builds the current folder into a binary
// watch https://youtube.com/shorts/jiVeI8WHiOs?feature=share

package main

import "fmt"

func main() {
    fmt.Println("simple")
    // Go syntax is minimal: no classes, no heavy abstractions.

    fmt.Println("fast-build")
    // Go compiles extremely fast compared to other languages.

    fmt.Println("instant-start")
    // Go binaries start in milliseconds with almost no delay.

    fmt.Println("lightweight")
    // Uses very little CPU and RAM; good for scaled systems.

    fmt.Println("efficient")
    // Executes code quickly and handles concurrency well.

    fmt.Println("compiled")
    // Produces a single self-contained binary with no VM needed.

    fmt.Println("portable")
    // Same binary can run on Linux, Windows, or macOS easily.
}


// Clone my repo go-O11y-0-pro, run this example, and tell me if it worked on your OS — Linux, Windows, or Mac.